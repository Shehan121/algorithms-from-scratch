"""Algorithms implemented from scratch, with their complexity measured.

The package is organised by problem family::

    algokit.sorting      nine sorts, from bubble to radix
    algokit.searching    linear, binary (three forms), interpolation, exponential
    algokit.structures   linked list, stack, queue, heap, BST, hash table, trie, union-find
    algokit.graphs       BFS, DFS, Dijkstra, topological sort, cycle detection, Kruskal
    algokit.dynamic      Fibonacci progression, LCS, edit distance, knapsack, LIS
    algokit.strings      naive, KMP, Rabin-Karp

and by measurement::

    algokit.instrument   count operations without touching the algorithms
    algokit.benchmark    timing and op-count sweeps over input shapes
    algokit.complexity   fit measurements against candidate growth curves
"""

__version__ = "1.0.0"
